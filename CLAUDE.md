# PDF Table Extractor - Medical Analysis Interpretation System

## 🎯 Project Overview

**Название:** PDF Table Extractor (первая часть более крупного проекта)

**Цель:** Медицинская экспертная система для автоматической интерпретации лабораторных анализов из PDF файлов с выдачей вероятностных диагностических гипотез.

**Технологии:**
- Backend: FastAPI (Python 3.11)
- PDF Processing: Camelot, pdfplumber
- Database: PostgreSQL (развёрнута на сервере 165.227.175.252)
- Infrastructure: Docker, Docker Compose, GitHub Actions CI/CD
- Orchestration: N8N (установлен на сервере, интеграция планируется)

---

## 🔄 Complete Workflow (End-to-End)

```
1. Загрузка PDF
   ↓
2. Парсинг файла (текущий код: app/main.py)
   ↓
3. Нормализация JSON (TODO)
   ↓
4. PostgreSQL логика (TODO)
   ↓
5. Вывод JSON с причинами (TODO)
   ↓
6. Вывод клиенту в "человеческом" виде (TODO)
```

### Детальное описание этапов:

#### Этап 1-2: Загрузка и парсинг PDF ✅
- **Статус:** Реализовано (текущий код в `app/main.py`)
- **Функционал:**
  - Endpoint: `POST /extract`
  - Двойная стратегия извлечения: lattice → stream (fallback)
  - Поиск заголовка "Параметр" в таблице
  - Обработка многострочных ячеек
  - Автоопределение колонок: параметр, результат, единица измерения
- **Output:** JSON с массивом analytes (raw_name, value, unit)

#### Этап 3: Нормализация JSON 🔨
- **Статус:** TODO
- **Задача:**
  - Принять JSON из этапа 2
  - Нормализовать `raw_name` → найти `analyte_id` через `analyte_aliases`
  - Нормализовать `unit` → найти `unit_id` через `unit_aliases`
  - Извлечь состояния пациента из шапки PDF (пол, возраст, беременность и т.д.)
- **Output:** Нормализованный JSON с ID из БД

#### Этап 4: PostgreSQL логика 🔨
- **Статус:** TODO
- **Задача:**
  - Для каждого показателя найти референс: `reference_ranges` + `reference_range_conditions`
  - Сравнить значение с референсом (формула ±5%)
  - Определить направление отклонения: HIGH/LOW
  - Через `analyte_cause_rules` найти возможные причины с весами
  - Агрегировать причины по всем показателям (суммировать веса)
- **Output:** JSON с причинами и весами

#### Этап 5-6: Вывод результатов 🔨
- **Статус:** TODO
- **Задача:**
  - Сформировать JSON с причинами
  - Преобразовать в человекочитаемый формат
  - Учесть `severity_level` из таблицы `causes`
- **Output:** Понятное объяснение для клиента

---

## 🗄️ Database Architecture

**Расположение:** PostgreSQL на сервере 165.227.175.252
**Схема:** См. `database/schema.sql`
**Статус:** БД развёрнута, таблицы созданы

### 9 таблиц в системе:

#### Справочники (Base Tables):
1. **analytes** - Эталонные показатели (total_bilirubin, hemoglobin, etc.)
2. **units** - Эталонные единицы измерения (mmol_l, g_l, etc.)
3. **conditions** - Состояния пациента (pregnancy, childhood, etc.)
4. **causes** - Медицинские причины отклонений (inflammation, liver_problems, etc.)

#### Нормализация (Alias Tables):
5. **analyte_aliases** - Альтернативные названия показателей (многие-ко-многим)
   - `raw_name`: как написано в PDF
   - `raw_name_normalized`: нормализованное для поиска
6. **unit_aliases** - Альтернативные единицы измерения
   - `raw_unit`: как написано в PDF
   - `raw_unit_normalized`: нормализованное

#### Медицинская логика:
7. **reference_ranges** - Референсные значения (нормы)
   - Контекстные: зависят от analyte_id, unit_id, sex, age_min/max
8. **reference_range_conditions** - Связь: какие условия влияют на норму (многие-ко-многим)
9. **analyte_cause_rules** - Вероятностная модель: показатель + направление → причина + вес
   - `direction`: HIGH или LOW
   - `weight`: 0.0-1.0 (вероятность)

### Ключевые связи:
```
analytes ← analyte_aliases (FK: analyte_id)
units ← unit_aliases (FK: unit_id)
analytes + units → reference_ranges (FK: analyte_id, unit_id)
reference_ranges ↔ conditions (через reference_range_conditions)
analytes + direction → causes (через analyte_cause_rules)
```

---

## 📁 Project Structure

```
pdf-table-extractor/
├── app/
│   ├── main.py              # FastAPI app (PDF extraction logic)
│   ├── requirements.txt     # Python dependencies
│   └── Dockerfile           # Container config
├── database/
│   └── schema.sql           # PostgreSQL DDL schema
├── .github/
│   └── workflows/
│       └── deploy.yml       # CI/CD: auto-deploy to server
├── docker-compose.yml       # Container orchestration
├── .gitignore
└── CLAUDE.md               # This file
```

---

## 🚀 Development Workflow

### Локальная разработка:
```bash
# Запуск локально
cd app
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# Или через Docker
docker compose up --build
```

### Деплой:
- **Автоматический:** Push в `main` → GitHub Actions → деплой на 165.227.175.252
- **Процесс:**
  1. SSH подключение к серверу
  2. `cd /root/pdf-table-extractor`
  3. `git pull origin main`
  4. `docker compose up -d --build`
- **Timeout:** 60 минут (недавно увеличен)

### Подключение к БД:
- **Инструмент:** DBeaver
- **Сервер:** 165.227.175.252
- **СУБД:** PostgreSQL
- **Пользователь:** (уточнить у Ivan)

---

## 📋 Current Status

### ✅ Что готово:
- [x] FastAPI микросервис для парсинга PDF
- [x] Логика извлечения таблиц из PDF (двойная стратегия: lattice + stream)
- [x] Docker контейнеризация
- [x] CI/CD пайплайн (GitHub Actions)
- [x] PostgreSQL база данных развёрнута
- [x] Схема БД спроектирована и создана

### 🔨 В разработке / TODO:
- [ ] **Улучшение PDF парсинга:**
  - Разные форматы PDF (разные лаборатории)
  - Вариативные названия параметров
  - Извлечение данных из шапки (пол, возраст, дата, лаборатория)
- [ ] **Этап 3: Нормализация JSON**
  - Сопоставление raw_name → analyte_id через нечёткий поиск
  - Сопоставление unit → unit_id
  - Извлечение и парсинг состояний пациента
- [ ] **Этап 4: PostgreSQL логика**
  - Поиск референсных значений
  - Сравнение с нормой
  - Определение причин через analyte_cause_rules
  - Агрегация весов причин
- [ ] **Этап 5-6: Вывод результатов**
  - Формирование JSON с причинами
  - Человекочитаемый формат
- [ ] **Заполнение БД данными:**
  - Справочники: analytes, units, conditions, causes
  - Алиасы: analyte_aliases, unit_aliases
  - Медицинские правила: reference_ranges, analyte_cause_rules
- [ ] **Интеграция с N8N** (оркестрация процесса)
- [ ] **Оптимизация БД:**
  - Индексы на FK и нормализованных полях
  - UNIQUE constraints на алиасах
  - condition_aliases таблица (по аналогии с остальными)

---

## 🎓 Important Notes

### PDF Парсинг:
- **Проблема:** PDF файлы из разных лабораторий имеют разную структуру
- **Решение:** Нужны примеры PDF для обучения парсера
- **Текущая логика:** Оптимизирована под один формат (с заголовком "Параметр")

### Нормализация названий:
- **Ключевая фишка:** `raw_name_normalized` и `raw_unit_normalized`
- **Подход:** lowercase + удаление пробелов/спецсимволов для нечёткого поиска
- **TODO:** Реализовать функции нормализации в Python коде

### Вероятностная модель:
- **Подход:** Веса причин суммируются по всем отклонённым показателям
- **Пример:**
  - Билирубин HIGH → Воспаление (0.6), Печень (0.4)
  - АЛТ HIGH → Воспаление (0.5), Печень (0.8)
  - **Итого:** Воспаление (1.1), Печень (1.2) → Печень более вероятна

### Референсные значения:
- **Контекстность:** Зависят от пола, возраста, состояний
- **Формула сравнения:** ±5% от границ референса
- **TODO:** Уточнить, используется ли строгое сравнение или нужны "серые зоны"

---

## 🔧 Technical Constraints

### Git & Deployment:
- **NEVER** push напрямую в main без review (если работаем в команде)
- **ALWAYS** тестируй локально перед push (автодеплой на продакшн!)
- Сервер: 165.227.175.252 - продакшн, осторожность с деструктивными командами

### Database:
- **NEVER** коммить саму БД в Git (только схема)
- **ALWAYS** используй миграции для изменения схемы (TODO: setup Alembic?)
- Схема в `database/schema.sql` - source of truth

### Code Style:
- Русские комментарии в коде - OK (медицинская терминология на русском)
- Названия переменных/функций - английские
- Docstrings - на русском, объясняют медицинскую логику

---

## 🌐 Integration Points

### N8N:
- **Расположение:** Установлен на сервере 165.227.175.252
- **Роль:** Оркестрация всего пайплайна (этапы 1-6)
- **TODO:** Спроектировать workflow в N8N
- **Интеграция:** Webhooks между FastAPI и N8N

### Future Expansions:
- Telegram бот для загрузки PDF?
- Web интерфейс для просмотра результатов?
- История анализов пользователя?
- Сравнение анализов во времени (тренды)?

---

## 🧪 Testing Strategy

### TODO: Создать тесты для:
1. PDF парсинга (unit tests на разных форматах)
2. Нормализации названий (fuzzy matching)
3. Логики поиска референсов
4. Вероятностной модели (суммирование весов)
5. End-to-end тестов (от PDF до вывода)

### Test Data:
- Нужна коллекция PDF файлов из разных лабораторий
- Эталонные результаты для сравнения

---

## 📞 Contacts & Resources

- **Разработчик:** Ivan (общается на русском)
- **Сервер:** 165.227.175.252 (SSH доступ)
- **GitHub:** (repository не указан в контексте)
- **Документация проекта:** Milanote (экспортировано в PDF)

---

## 💡 Ideas for Future

1. **Machine Learning:**
   - Обучить модель на исторических данных для улучшения весов причин
   - Автоматическое извлечение паттернов из PDF

2. **Multi-language Support:**
   - Поддержка английских анализов
   - Автоопределение языка PDF

3. **Visualization:**
   - Графики показателей (в норме/вне нормы)
   - Сравнение с референсами визуально

4. **Recommendations:**
   - Не только причины, но и рекомендации (к какому врачу обратиться)
   - Связь с базой знаний медицинских протоколов

---

**Last Updated:** 2026-02-15
**Version:** 0.1.0 (MVP в разработке)
